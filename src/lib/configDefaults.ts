export const DEFAULT_MAX_ACTIVE_USERS = Number.POSITIVE_INFINITY;

export const DEFAULT_FREE_LIMITS = {
  maxRequests: 60,
  maxUnits: 120_000,
};

export const DEFAULT_PRO_LIMITS = {
  maxRequests: 600,
  maxUnits: 1_200_000,
};

export const DEFAULT_GLOBAL_LIMITS = {
  maxRequests: 6_000,
  maxUnits: 12_000_000,
};
